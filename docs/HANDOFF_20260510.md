# Session Handoff: Multistate Portability Sprint

**Session window:** 8 May 2026 to 10 May 2026
**Status at handoff:** Multistate pipeline validated end to end; three production runs queued and running overnight.

## One paragraph summary

The fia-cem-maine projection pipeline now runs on Minnesota, Washington, and Georgia in addition to the original Maine baseline. All three non Maine smoke tests passed cleanly in 17 minutes combined wall time, producing biophysically defensible per state numbers (WA 3,004 cuft/ac matching Pacific Northwest reality, MN 1,221 matching Lake States literature, GA 1,205 reflecting southern pine plantation forestry). The HCB ownership × EPA Omernik Level III ecoregion cross stratification is the canonical multistate scheme; the foundation data (104,628 plot crosswalk, 22 L3 ecoregion SDImax lookup, ClimateNA input CSVs for all four states) is in place. A pre existing gr_ratio reporting bug affects all states equally and is partially fixed; full reconciliation needs a one day audit of R/03_harvest_choice.R line 409. Three production grade runs (--n_sims 100, --cycles 15, full 5 scenario harvest set) are queued on Cardinal as jobs 9327152 (MN), 9327153 (WA), 9327155 (GA) and should complete within roughly 4 to 16 hours each.

## What's running on Cardinal right now

| Job | State | Wall budget | Memory | Started |
|---|---|---|---|---|
| 9327152 | MN | 20 hrs | 200 GB | PENDING |
| 9327153 | WA | 12 hrs | 180 GB | PENDING |
| 9327155 | GA | 16 hrs | 200 GB | PENDING |

Each writes to `output/<STATE>_<DATE>_rcp45_wear_p1/` and follows the same artifact pattern as the May 5 ME r21 run: ci_summaries.csv, raw_mc_summaries.csv, table_inventory_summary.csv, table_gr_ratios.csv, five PNG figures, and per_plot_projections.rds.

To check progress in any future session:
```
ssh crsfaaron@cardinal.osc.edu
squeue -u crsfaaron -h -O jobid,state,name:25,timeused | grep p1
sacct --starttime "2026-05-10" --format=JobID,JobName%25,State,ExitCode,Elapsed | grep p1
```

To see partial output as runs progress:
```
tail -30 ~/fia_cem_projections/logs/fia_mn_p1_*.out
ls -la ~/fia_cem_projections/output/MN_20260510_rcp45_wear_p1/
```

## Where things live

### Local repo

`~/Documents/Claude/CRSF-Cowork/active-projects/fia-plot-matching/` (note the hyphenated name and CRSF-Cowork parent). The repo is a clone of `github.com/holoros/fia-cem-maine`. Sixteen commits ahead of origin/main at session end:

```
bd48a2c  docs: update gr_ratio bug analysis after partial fix validation
bc0a2cf  fix: gr_ratio units mismatch at 06_projection_engine.R line 922
40b1e5a  docs: MEMORY GA smoke COMPLETED (3 of 3 non-Maine tests pass)
1d5adec  docs: MEMORY session #7 entry multistate portability landed
e02c02c  scripts: WA + GA smoke-test submit scripts
45f823f  fix: bump download.file timeout to 3600s in donor-state driver
d47758e  docs: progress log for MULTISTATE_PORTABILITY_GAPS.md
d58bf75  scripts: serial donor-state download driver for MN/WA/GA cohorts
a94d53b  scripts: build_fia_rds_from_entire.R (login-node RDS builder)
c1cd54a  code: externalize Maine constants from R/06 (Section 8 step 6)
1894b3f  data: SDImax-by-L3-ecoregion lookup for ME, MN, WA, GA
4c53e5f  fix: correct L3 shapefile filename to us_eco_l3.shp
5c33163  scripts: repoint to ~/FIA/ENTIRE_*.csv and add ClimateNA-input extractor
768e018  scripts: MN smoke-test submit script
bc2f443  code: Section 1 switch-table edits for MN, WA, GA
e3ebd8d  scripts: HCB x L3 ecoregion plot crosswalk for ME/MN/WA/GA
6b660e4  docs: multistate portability gap audit (MN, WA, GA)
```

To push (session sandbox can't because HTTPS remote needs your GitHub credentials):
```
cd ~/Documents/Claude/CRSF-Cowork/active-projects/fia-plot-matching
git push origin main
```

### Cardinal

Project root: `~/fia_cem_projections/` (account PUOM0008, user crsfaaron, login: cardinal.osc.edu)

| Path | Purpose |
|---|---|
| `~/fia_cem_projections/scripts/` | New scripts from this session: build_hcb_l3_crosswalk.R, extract_plot_locations.R, build_sdimax_l3_ecoregion_table.R, build_fia_rds_from_entire.R, download_donor_states.sh |
| `~/fia_cem_projections/osc/submit_{mn,wa,ga}_production.sh` | Production submit scripts queued today |
| `~/fia_cem_projections/osc/submit_{mn,wa,ga}_smoke.sh` | Smoke submit scripts (all passed) |
| `~/fia_cem_projections/config/state_constants.csv` | Per state climate/disturbance/SDImax defaults |
| `~/fia_cem_projections/config/fia_plots_hcb_l3.csv` | 104,628 row foundation crosswalk |
| `~/fia_cem_projections/config/sdimax_by_l3_ecoregion.csv` and four siblings | Multistate SDImax stratification |
| `~/fia_cem_projections/R/06_projection_engine.R` | Patched with state_constants helper + gr_ratio Layer 1 fix |
| `~/fia_cem_projections/output/MN_20260510_mn_smoke/` | MN smoke output (28 MB, includes per_plot_projections.rds) |
| `~/fia_cem_projections/output/WA_20260510_wa_smoke/` | WA smoke output (5.8 MB) |
| `~/fia_cem_projections/output/GA_20260510_ga_smoke/` | GA smoke output (39 MB) |
| `~/fia_cem_projections/output/ME_20260508_rcp85_hadgem2_wear_r21/` | ME r21 RCP 8.5 reference |
| `~/FIA/fia_db_{MN,WA,GA}.rds` | Per state RDS caches on scratch (no quota); symlinked into ~/fia_data/ |
| `~/FIA/climate/plot_locations_<STATE>.csv` and climatena_input_<STATE>.csv | Ready for ClimateNA external processing |
| `~/FIA/download_logs/master.log` | Donor download driver log |

Backups from this session:
- `~/fia_cem_projections/R/06_projection_engine.R.preupdate.20260509b` (pre Section 6 externalization)
- `~/fia_cem_projections/R/06_projection_engine.R.preupdate.20260510_grfix` (pre gr_ratio Layer 1 fix)
- `~/fia_cem_projections/R/10_state_expansion.R.preupdate.20260509` (pre Section 1 switch edits)
- `~/fia_cem_projections/run_projection.R.preupdate.20260509` (pre Section 1 edits)
- `~/fia_cem_projections/osc/submit_rcp45_wear_r21.sh.preupdate.20260508` (pre polished version lift)

Cardinal quota at handoff: 436 GB / 500 GB. Inodes: 849k / 1000k.

### Archived

`/fs/scratch/PUOM0008/crsfaaron/archive/`:
- `output_archive_20260508/` (30 superseded output runs, ~21 GB)
- `code_baks_20260508/` (16 .bak.* files)
- `osc_archive_20260508/` (112 superseded submit scripts)

## What was accomplished

### Section 8 progress on MULTISTATE_PORTABILITY_GAPS.md

| Step | Status | Notes |
|---|---|---|
| 1. HCB × L3 plot crosswalk | DONE | 104,628 rows; 22 ecoregions touched; per state HCB-FIA agreement 41-74% |
| 2. Per state FIA RDS caches | DONE | MN 1.4 GB, WA 586 MB, GA 1.7 GB on scratch; symlinked into ~/fia_data |
| 3. Plot locations + ClimateNA inputs | DONE | All four states; ClimateNA itself runs externally |
| 4. SDImax by L3 ecoregion | DONE | 22 L3 × typgroups; PNW peaks (~1300), prairie troughs (~700) |
| 5. Switch-table edits | DONE | bc2f443: state_name, STATECD, donor_states extensions for MN/WA/GA |
| 6. Externalize Maine constants from R/06 | DONE | c1cd54a: config/state_constants.csv keyed by state |
| 7. Per state county harvest logit offset | NOT STARTED | Needs upstream FIA harvest event analysis per state |
| 8. Smoke test runs | DONE | All three pass; MN 5:36, WA 1:53, GA 9:59 |
| 9. Production runs | QUEUED | Jobs 9327152/9327153/9327155 submitted, pending start |

### Out of scope discoveries

- gr_ratio = 0.001 in raw_mc_summaries.csv was a pre existing bug, not multistate specific. ME r21 had the same value. Layer 1 fix (line 922) confirmed working (gr_ratio moved 0.001 → 0.006 for MN, exactly the 1/harvest_rate factor predicted). Layer 2 remains in R/03_harvest_choice.R line 409.

- ~/FIA/ENTIRE_TREE.csv is missing on Cardinal; only ENTIRE_TREE_GRM_THRESHOLD.csv and ENTIRE_TREE_WOODLAND_STEMS.csv exist. Per state TREE.csv files in ~/fia_data/ are the canonical TREE source.

- Cardinal's L3 ecoregion shapefile is ~/Disturbance/us_eco_l3.shp (full set). The us_eco_l3_state_boundaries.dbf is a standalone attribute file with no geometry.

- The curated repo's R/06_projection_engine.R is intentionally the older r20 baseline; cem_pipeline_patch/06_projection_engine.R has the current r21 / v4 productivity multiplier patch. Cardinal copies the patched version over R/ at deployment.

- Cardinal hosts other active projects (cbm_maine, fvs-modern, LSOG, landis2HPC). They share the same quota and may be running their own jobs in the same queue. The me_state / me_agg / me_pub job names belong to cbm_maine, not fia-cem-maine, and I conflated them early in the session.

## How to resume

### Tomorrow morning (after production runs complete)

1. Check Cardinal queue and recent jobs:
   ```
   ssh crsfaaron@cardinal.osc.edu
   sacct --starttime "2026-05-10 19:00" --format=JobID,JobName%25,State,ExitCode,Elapsed | grep p1
   ```

2. Pull production outputs locally for review:
   ```
   cd ~/Documents/Claude/CRSF-Cowork/active-projects/fia-plot-matching
   mkdir -p output/MN_<DATE>_rcp45_wear_p1 output/WA_<DATE>_rcp45_wear_p1 output/GA_<DATE>_rcp45_wear_p1
   rsync -av crsfaaron@cardinal.osc.edu:fia_cem_projections/output/MN_*_rcp45_wear_p1/ output/MN_*_rcp45_wear_p1/
   # same for WA and GA
   ```

3. Compare each state's table_inventory_summary.csv against published FIA EVALIDator totals for that state:
   - MN forest area ~17.4 million acres, AGC ~700 TgC, volume ~28 Bcuft
   - WA forest area ~22.0 million acres, AGC ~1,100 TgC, volume ~70 Bcuft
   - GA forest area ~24.8 million acres, AGC ~700 TgC, volume ~32 Bcuft

4. If numbers look reasonable, push commits and write the next session entry in docs/MEMORY.md.

### Next-priority forward work

In rough order of value to manuscript:

1. **Audit R/03_harvest_choice.R line 409** to resolve the gr_ratio Layer 2 issue. Trace whether `volcfnet` and `tpa_live` in the intensity formula are per acre or per tree, then either fix the formula or convert downstream. A correct gr_ratio is publication-quality important since the figure has been a manuscript headline metric.

2. **Run ClimateNA externally** on the four `~/FIA/climate/climatena_input_<STATE>.csv` files. Drop outputs into `~/FIA/climate/<STATE>/normals/` and `~/FIA/climate/<STATE>/<ssp>/`. Adapt `R/08_climate_interface.R` to look there. Unblocks `--use_decoupled_climate` for non Maine.

3. **Layer the RCP 8.5 production runs** alongside the RCP 4.5 ones already queued. Same submit pattern, just `--climate_rcp 8.5` and `--tag rcp85_wear_p1`. Doubles the per state output but matches the ME r21 dual RCP pattern.

4. **Build per state county harvest logit offset** (Section 8 step 7). Maine version was built via `scripts/build_county_harvest_lookup.R` reading a pre computed harvest_rate_per_yr per county. For MN/WA/GA, would need to derive harvest event rates from FIA TREE_GRM_COMPONENT by county. Roughly a one to two day analytical task per state.

5. **Validate the per state climate constants** in config/state_constants.csv. Current values are NCA5-derived estimates. Worth cross-checking against MACA/CMIP6 ensemble means for each state's bounding box before publication-quality runs.

6. **Per state policy and land use scenario sets** (R/05). Maine has `maine_policy_scenarios()` and `maine_land_use_scenarios()`. MN, WA, GA each have their own policy levers (Minnesota Forests for the Future, Washington Forestry Industries Carbon, Georgia conservation easements). Worth building state-specific scenario sets if the manuscript wants policy comparison.

7. **Push 2 unpushed local commits to GitHub** (the gr_ratio fix and its analysis update). One-line git push.

### Sandbox limitations to remember in next session

- Each bash call runs in an isolated sandbox; SSH keys must be re-staged each call from `/sessions/eloquent-elegant-brahmagupta/mnt/uploads/cardinal_key` to `/tmp/cardinal_key`.
- The local repo lives under `/sessions/eloquent-elegant-brahmagupta/mnt/Claude/CRSF-Cowork/active-projects/fia-plot-matching/` (sandbox view) which maps to `/home/aweiskittel/Documents/Claude/CRSF-Cowork/active-projects/fia-plot-matching/` (host view).
- Git push via HTTPS requires GitHub credentials the sandbox doesn't have; user must push from workstation.
- ClimateNA is a desktop GUI tool; sandbox can prep the input CSVs but not run the tool.
- Cardinal scp from sandbox sometimes times out at the wrapper level even when the file copy succeeds. Re-check on the destination side rather than treating bash timeout as failure.

## Key file references

For a future session needing to understand the codebase:

- `docs/MULTISTATE_PORTABILITY_GAPS.md` — the canonical audit doc; Section 11 progress log tracks what's done; Section 12 documents findings discovered during implementation.
- `docs/MEMORY.md` — session-by-session running log of the entire project history; Session #7 entry covers this multistate sprint.
- `docs/LANDOWNER_INTEGRATION_STRATEGY.md` — strategy proposal (status: planning) for the HCB landowner stratification scheme; the cross-state crosswalk implemented in this session realizes the Phase 1 deliverable.
- `cem_pipeline_patch/06_projection_engine.R` — the live (Cardinal-deployed) projection engine. Section 6 externalization at lines ~115-160; gr_ratio Layer 1 fix at line ~932.
- `R/10_state_expansion.R` — state-aware switches; Section 1 edits at lines 113-118 (state_name) and 173-176 (statecd).
- `config/state_constants.csv` — per state climate/disturbance/SDImax defaults; add new states by appending a row.

## Open questions for the user

1. Do the per state state_constants.csv values look reasonable, especially the WA wildfire baseline of 0.060/cycle (10× ME) and the terminal_age values? These are educated estimates that may benefit from your domain check.

2. For the manuscript, is the multistate work intended as a separate paper, a section of the existing manuscript, or a companion piece? That affects how aggressively to harden the docs and tables.

3. Production-grade RCP 8.5 runs alongside the 4.5 runs queued today — should those fire automatically after 4.5 completes, or wait on review?

4. Is there a target deadline for getting the cross-state results into shareable form (e.g., advisory board meeting, conference abstract)?
