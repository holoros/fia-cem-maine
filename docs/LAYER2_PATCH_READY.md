# Layer 2 patch ready to apply

*Status note generated 10 May 2026.*

The Layer 2 gr_ratio patch documented in `docs/GR_RATIO_LAYER2_AUDIT.md` is ready to apply at `R/03_harvest_choice.R` lines 409 and 414. The fix removes the `tpa_live` factor from the `vol_removed_total` calculation and the `saw_fraction` denominator so that `volcfnet`, which is already cuft per acre via the per tree aggregation in `R/01_data_prep.R` line 149 (`sum(TPA_UNADJ * VOLCFNET)`), is no longer multiplied by trees per acre a second time. The change is two minimal edits inside `estimate_removals()` and does not touch the function signature, the harvest choice logit, or anything downstream of `vol_removed_total` other than the values it produces. Trigger condition: the patch should land before the next ME r21 economic overlay rerun (any run with `--use_maine_econ`), since that is the only flag path that exercises `estimate_removals()`. The six currently running multistate p1 jobs all use `--no_econ` and `--skip_supply` so they are unaffected. Regression test after applying: deploy the patched `R/03_harvest_choice.R` to Cardinal at `~/fia_cem_projections/R/` with a backup tagged `.preupdate.20260511_layer2`, then submit a 1 sim, 3 cycle ME smoke with `--use_maine_econ` and `--scenario_set bau`; the resulting `table_gr_ratios.csv` should land in the 0.5 to 3.0 range for cycle 1 BAU, confirming that gr_ratio has been restored to its expected biological magnitude. If the smoke passes, propagate the same edit to `cem_pipeline_patch/06_projection_engine.R` if it mirrors `R/03` (it does not currently, but check), then schedule a full ME r21 econ rerun (`submit_rcp45_wear_econ_r21.sh` and the RCP 8.5 sibling) to produce the manuscript ready figures with the corrected gr_ratio.
