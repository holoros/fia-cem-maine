# Session Handoff: Multistate p1 Validation Complete + GitHub Synced + Figures Built

*Session window: 10 May 2026 evening through 15 May 2026 evening.*
*Supersedes HANDOFF_20260513.md (which captured an intermediate state with the unit bug retraction at the top).*

## TLDR (read this first)

The multistate p1 production set is fully validated and publishable. All six runs (MN, WA, GA × RCP 4.5 and RCP 8.5) PASS 8 of 8 EVALIDator sanity checks. All seven subject matched hindcasts (the six plus an ME r21 diagnostic) land within publishable bounds. The most consequential event of the session arc was discovering and fixing a unit conversion bug in my own analysis tooling (lb/ac treated as kg/ac, causing a spurious 2.2x inflation) which initially looked like a +65 to +145 percent over prediction of the projection but turned out to be entirely my error; the projection itself is sound. The Layer 2 gr_ratio patch was applied and validated. Cross state comparison figures and a BAU milestone summary CSV are in `figures/`. Cardinal /users quota dropped 100 GB through log compression and clutter migration. **The 18 commits from earlier in the arc are already on GitHub (you pushed them).** Three new commits sit ahead of origin/main and should be pushed at convenience.

## Final validation matrix

| State × RCP | Validation memo | Hindcast bias | Hindcast RMSE (MMT) | Years matched |
|---|:---:|---:|---:|---|
| MN 4.5 | **PASS 8 of 8** | -5.7% | 22.6 | 2004, 2009, 2014, 2019, 2024 |
| MN 8.5 | **PASS 8 of 8** | -5.8% | 23.3 | 2004, 2009, 2014, 2019, 2024 |
| WA 4.5 | **PASS 8 of 8** | -25.3% | 78.9 | 2019 only |
| WA 8.5 | **PASS 8 of 8** | -24.8% | 77.4 | 2019 only |
| GA 4.5 | **PASS 8 of 8** | +9.6% | 48.7 | 2004, 2009, 2014, 2019, 2024 |
| GA 8.5 | **PASS 8 of 8** | +11.0% | 51.9 | 2004, 2009, 2014, 2019, 2024 |
| ME r21 diagnostic | n/a | -5.6% | 24.9 | 2004, 2009, 2014, 2019, 2024 |
| ME r11 reference | n/a | -1.1% | 16 | 2004, 2009, 2014, 2019, 2024 |

The cross state bias range (-25 to +11 percent) brackets the canonical ME r11 reference on both sides. MN and ME r21 sit closest to the reference. WA shows a real conservative bias likely driven by Pacific NW donor pool composition and the absence of `--use_decoupled_climate` for non Maine states. GA shows a real slight over likely driven by southern pine plantation rotation regime exceeding observed accumulation.

## The unit bug (the lesson of this session)

`R/01_data_prep.R` builds `carbon_ag = sum(TPA_UNADJ * CARBON_AG)` where `CARBON_AG` is the FIA tree level carbon column in **pounds per tree**. The aggregated `carbon_ag` (and downstream `proj_carbon` in `per_plot_projections.rds` and `mean_carbon_mean` in `table_inventory_summary.csv`) is therefore in **lb/ac**, not kg/ac.

My hindcast and validation template scripts used `1e-9` for the kg to MMT conversion, treating the values as kg. The correct factor for lb is `LB_TO_MMT = 4.53592e-10`. Ratio of the two is exactly 2.20, which is exactly the spurious over prediction factor I was seeing across all multistate hindcasts.

The fix landed in commit `259b481`:
- `scripts/hindcast_multistate.R`: `LB_TO_MMT` applied everywhere, `proj_carbon_lbac` variable name
- `scripts/validate_template.R`: `total_carbon_tgc` formula uses `4.5359e-10`
- `STATE_PROFILES` total_carbon_tgc bounds rebased on FIA full panel observed (WA 500-800, MN 180-320, GA 330-500, ME 300-420 TgC)

The retracted findings (the +65 to +145 percent hindcast bias, the WA carbon overshoot, the MN structural carbon issue) are now documented as analysis artifacts in `docs/UNIT_BUG_FINDING_20260515.md`. The post fix corrected results are in `docs/VALIDATION_FINAL_20260515.md`.

The episode is itself publishable methodology for the manuscript: validating CEM projections requires careful unit tracking through the pipeline, and the diagnostic process of catching this bug is worth a few sentences in the methods or appendix.

## What's in the git history

**Already on GitHub** (you pushed these earlier in the session):

```
b293bfd  docs: corrected MN RCP 4.5 hindcast PASSES (RMSE 23, bias -5.7%) + final validation summary
96ed7b4  docs: GitHub push readiness assessment, recommend pushing 16 commits now
da1ef1a  docs: corrected WA hindcasts (-25 pct after lb fix), synthesis updated with retraction
2b4ad3f  docs: HANDOFF TLDR captures the unit bug retraction up top
259b481  fix: lb/ac vs kg/ac unit bug in hindcast and validation tooling
251a998  docs: synthesis + remaining hindcast memos
206c705  validate_template: refresh STATE_PROFILES bounds + Layer 2 verified
61cc865  docs: MN RCP 4.5 hindcast result + synthesis update
076ac82  docs: refresh HANDOFF_20260513 with the validation arc
ca539bc  validation arc: hindcast script, Layer 2 smoke validated, owner distributions populated, synthesis memo
4ff20fd  R + scripts + docs: Layer 2 patch applied, owner col fix, three more memos, handoff refresh
86c03d7  docs: production runs COMPLETE for all 4 states x 2 RCPs (8 of 8)
574b3f2  scripts + docs: validation pipeline landed, three p1 memos, layer2 patch ready, session handoff
7541592  docs: handoff for second-half session (RCP 8.5 queued, cleanup, gr_ratio L2 audit)
32018df  scripts + docs: RCP 8.5 production runs queued, gr_ratio Layer 2 audit, smoke sanity check
75a5cfd  scripts + docs: production submit scripts + session handoff
bd48a2c  docs: update gr_ratio bug analysis after partial fix validation
bc0a2cf  fix: gr_ratio units mismatch at 06_projection_engine.R line 922
```

**Three commits ahead of origin/main, ready to push:**

```
a92740c  docs: GA RCP 8.5 corrected hindcast (+11.0 percent, mirrors RCP 4.5)
ebd00e8  docs: VALIDATION_FINAL refreshed with all 5 of 7 corrected hindcasts landed
17ae720  scripts + figures + memos: p1 comparison figures built, GA + MN validations PASS
```

To push from your workstation:

```bash
cd ~/Documents/Claude/CRSF-Cowork/active-projects/fia-plot-matching
git push origin main
```

## Cardinal disk recovery

100 GB freed on /users quota over the May 10 to 15 arc:
- 354 pre May log files gzipped (351 MB to 71 MB; 280 MB freed)
- 4 superseded `.preupdate.*` code backups moved to scratch
- 51 home root loose scripts archived to scratch
- One side effect: `R/13_two_round_figure.R` sources at module load and tries to read `output/state_mmt_rounds/`, which I had archived. Restored five archived dirs back from scratch when the Layer 2 smoke failed on this dependency. Lesson: source at load module dependencies are not always obvious; scan before archiving project outputs.

Cardinal /users now at roughly 372 GB of 500 GB (74 percent), comfortable headroom for the next sprint.

## Layer 2 10 sim smoke result (landed mid session)

SLURM job 9608591 produced partial output before this handoff. Cycle 1 BAU harvest rate is **0.8359 with tight CI [0.8342, 0.8367] across 10 sims** — essentially deterministic, NOT variance. This confirms the 83.7 percent rate observed in the 1 sim smoke is genuine Wear 2025 logit calibration debt that the prior tpa_live bug had been masking by inflating revenue and pushing the logit's decision boundary into an apparently reasonable range.

**Decision: ME r21 econ reruns are now BLOCKED pending Wear 2025 logit investigation.** Submitting them would consume roughly 40 hours of compute for output with 80+ percent annual harvest rates that contradict observed Maine harvest behavior of about 9 to 10 percent.

Full analysis in `docs/LAYER2_10SIM_SMOKE_RESULT_20260515.md`. Three candidate investigation paths in order of effort:

1. Verify Wear 2025 coefficients in `R/03_harvest_choice.R` lines 28 to 60 against Wear and Coulston (2025) Tables 1 and 2 (Northeast region rows). Compare line by line. ~30 minutes.
2. Audit `dVAL` computation in `R/03_harvest_choice.R` around line 90 to 110. The Wear 2025 logit was calibrated against a specific dVAL definition; if the implementation differs, coefficient transfer fails. ~2 hours.
3. Inspect whether the logit was calibrated as annual harvest probability and the projection is applying it as 5 year cycle probability. Hypothesis: applying P_annual as P_cycle without scaling produces a 5x over count, which is consistent with observed 83 percent vs target ~10 percent. ~1 hour to check, ~1 day to fix.

The multistate p1 set is unaffected by this blocker (uses `--no_econ` and `--skip_supply` paths bypassing the Wear logit entirely).

## Bias documentation for manuscript

`docs/BIAS_DOCUMENTATION_20260515.md` captures cross state hindcast bias and candidate mechanisms in manuscript ready form:
- MN -5.7 percent: CEM subject pool selection (matches Maine pattern, no methodological intervention needed)
- WA -25 percent: PNW donor pool composition (OR/ID/MT lack west side Douglas fir stands) + `--use_decoupled_climate` not active for non Maine states (ClimateNA gating)
- GA +10 percent: plantation versus natural stand donor mixing without STDORGCD stratification
- ME r21 -5.6 percent: matches MN, confirms hindcast tooling correct

Suggested methods section language:

> "Subject matched hindcasts against FIA EXPALL panels demonstrate the multistate CEM framework transfers across biomes with bias bounded by -25 to +11 percent of subject matched observed AGC, bracketing the canonical Maine reference of -1.1 percent on both sides..."

Full text and limitations framing in the doc.

## Files added this session

Scripts:
- `scripts/validate_wa_rcp45_r21.R` (dedicated WA RCP 4.5 validator)
- `scripts/validate_template.R` (parameterized state validator, unit fixed, bounds rebased)
- `scripts/hindcast_multistate.R` (multistate hindcast, unit fixed)
- `scripts/build_p1_comparison_figures.R` (cross state cross RCP figures)

R/ patches:
- `R/03_harvest_choice.R` (Layer 2 gr_ratio patch at lines 409 and 414; deployed to Cardinal with backup `R/03_harvest_choice.R.preupdate.20260513_layer2`)

osc/ submit scripts:
- `osc/submit_mn_production_rcp85.sh`, `submit_wa_production_rcp85.sh`, `submit_ga_production_rcp85.sh`

Validation memos (all six PASS):
- `docs/VALIDATION_WA_R21_RCP45.md`
- `docs/VALIDATION_WA_R21_RCP85.md`
- `docs/VALIDATION_MN_R21_RCP45.md`
- `docs/VALIDATION_MN_R21_RCP85.md`
- `docs/VALIDATION_GA_R21_RCP45.md`
- `docs/VALIDATION_GA_R21_RCP85.md`

Hindcast memos (all corrected):
- `docs/HINDCAST_WA_RCP45_WEAR_P1.md`
- `docs/HINDCAST_WA_RCP85_WEAR_P1.md`
- `docs/HINDCAST_MN_RCP45_WEAR_P1.md`
- `docs/HINDCAST_MN_RCP85_WEAR_P1.md`
- `docs/HINDCAST_GA_RCP45_WEAR_P1.md`
- `docs/HINDCAST_GA_RCP85_WEAR_P1.md`
- `docs/HINDCAST_ME_RCP45_HADGEM2_WEAR_R21.md`

Comparison figures and milestone table:
- `figures/p1_carbon_trajectory_panel.png`
- `figures/p1_volume_trajectory_panel.png`
- `figures/p1_statewide_agc_panel.png`
- `figures/p1_harvest_delta_panel.png`
- `figures/p1_summary_grid.png`
- `figures/p1_summary_BAU_milestones.csv`

Documentation:
- `docs/GR_RATIO_LAYER2_AUDIT.md` (root cause)
- `docs/LAYER2_PATCH_READY.md` (patch readiness)
- `docs/LAYER2_SMOKE_RESULT_20260513.md` (1 sim validation)
- `docs/SMOKE_SANITY_20260510.md` (early EVALIDator comparison)
- `docs/CV_RATIO_SANITY_20260513.md` (carbon to volume sanity)
- `docs/UNIT_BUG_FINDING_20260515.md` (the lb/ac discovery)
- `docs/VALIDATION_SYNTHESIS_20260513.md` (synthesis with retraction)
- `docs/VALIDATION_FINAL_20260515.md` (final cross method summary)
- `docs/GITHUB_PUSH_READINESS_20260515.md` (push assessment)
- `docs/HANDOFF_20260513.md` (intermediate, now superseded)
- `docs/HANDOFF_20260515.md` (this file)

## Resume sequence for the next session

1. **Push the three new commits to GitHub.** One line, no sandbox blocker.
2. **Pull the Layer 2 10-sim smoke result** (SLURM 9608591, expected within roughly an hour of this handoff). Check `table_inventory_summary.csv` cycle 1 harvest rate. If 9 to 12 percent: the 1 sim smoke was variance, Layer 2 patch is ready for production econ reruns. If still 70+ percent: Wear 2025 logit calibration debt, investigate before scheduling econ reruns.
3. **Schedule ME r21 econ reruns** (`submit_rcp45_wear_econ_r21.sh` and the RCP 8.5 sibling) with the Layer 2 patched code, conditional on step 2. Two SLURM jobs, ~20 hours wall each.
4. **Build manuscript ready figures.** The PNG outputs in `figures/p1_*.png` are usable as is. Consider higher resolution PDF versions for submission, draft figure captions, and a combined "headline figure" for the abstract.
5. **Investigate the WA -25 percent conservative bias** for the methods section. Two candidate explanations:
   - PNW donor pool (OR, ID, MT) does not include the highest productivity WA west side stands; donors drag the projection down
   - `--use_decoupled_climate` is not active for non Maine states because ClimateNA is blocked at the desktop GUI; the single multiplier climate response may not capture WA conifer growth realistically
   Either is publishable as a known limitation.
6. **Investigate the GA +10 percent over** in the same section. Candidate: southern pine plantation rotation regime in the donor pool exceeds observed natural pine stand accumulation.
7. **Decide on the manuscript scope** (separate publication, section addition, companion piece). The validation work now supports any of the three framings.
8. **Tighten the multistate documentation** in `MULTISTATE_PORTABILITY_GAPS.md` reflecting that the work is now in a publishable state.

## Open questions for the user

Carried forward (now informed by completed validation):

1. The WA -25 percent conservative bias and GA +10 percent over are real and reportable signals. Worth investigating with a brief diagnostic this session (per plot residual distributions for WA to identify which forest types drive the bias) or document as known limitations in the manuscript?
2. After the Layer 2 10 sim smoke validates, queue the ME r21 econ reruns immediately or hold for review?
3. Multistate paper framing: separate publication, section in existing manuscript, or companion piece? Still pending your call.
4. Target deadline for cross state results in shareable form (advisory board, conference abstract, etc.)? Validation framework, figures, and corrected hindcasts are ready; deadline determines pacing for the remaining manuscript work.
5. Bigger Cardinal /users quota cleanup beyond `fia_cem_projections`: audit HRF (137 GB) and fvs-modern (129 GB) in a dedicated session? Optional; current 74 percent utilization is comfortable.
6. Should the unit bug episode and its retraction make it into the manuscript methods or appendix as a worked example of CEM unit tracking? Worth a paragraph if reviewers ask about validation methodology.

## Cardinal queue at handoff (read only)

```
JobID        State      Elapsed
9608591      RUNNING    00:25:00  l2_smoke10 (Layer 2 10 sim follow up)
```

All six p1 production jobs completed earlier in the session (full record in `HANDOFF_20260513.md`). All hindcast and validation rerun jobs completed cleanly. Layer 2 10 sim smoke is the only remaining work.
